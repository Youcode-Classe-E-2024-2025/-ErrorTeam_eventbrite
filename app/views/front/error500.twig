<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erreur 500 - Erreur interne du serveur</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Animation pour le tremblement */
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            10%, 30%, 50%, 70%, 90% { transform: translateX(-10px); }
            20%, 40%, 60%, 80% { transform: translateX(10px); }
        }
        /* Animation pour le pouls */
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg text-center animate-fadeIn">
        <!-- Icône -->
        <div class="text-red-500 text-6xl mb-4 animate-pulse">
            <i class="fas fa-exclamation-circle"></i>
        </div>

        <!-- Titre -->
        <h1 class="text-5xl font-bold text-red-500 mb-4 animate-shake">500</h1>

        <!-- Message d'erreur -->
        <p class="text-lg text-gray-700 mb-4">Oups ! Une erreur interne s'est produite sur le serveur.</p>
        <p class="text-lg text-gray-700 mb-6">Veuillez réessayer plus tard ou contacter l'administrateur du site.</p>

        <!-- Lien de retour -->
        <a href="/" class="inline-block bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 transition-transform transform hover:-translate-y-1">
            Retourner à la page d'accueil
        </a>
    </div>
</body>
</html>
