<!DOCTYPE html>
<html>
<head>
    <title>Réserver un événement</title>
</head>
<body>
    <h1>Réserver l'événement: {{ event.getTitle() }}</h1>
    <p>Places disponibles : {{ event.getAvailableSeats() }}</p>
    <form action="/events/{{ event.getId() }}/reservations/create" method="post">
        <label for="number_of_tickets">Nombre de billets:</label>
        <input type="number" id="number_of_tickets" name="number_of_tickets" min="1" max="{{ event.getAvailableSeats() }}" required><br><br>
        <input type="submit" value="Réserver">
    </form>
</body>
</html>