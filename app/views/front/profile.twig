<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Profil</title>
    <link rel="stylesheet" href="/assets/css/style.css"> <!-- Assurez-vous que le chemin est correct -->
</head>
<body>
    <h1>Profil Utilisateur</h1>

    {% if session.success %}
        <div class="success">{{ session.success }}</div>
        {% do session.remove('success') %}
    {% endif %}

    {% if session.error %}
        <div class="error">{{ session.error }}</div>
        {% do session.remove('error') %}
    {% endif %}

    <div class="profile-info">
        <img src="{{ user.avatar ? user.avatar : '/assets/img/default_avatar.png' }}" alt="Avatar" width="100">
        <p>Nom d'utilisateur: {{ user.username }}</p>
        <p>Email: {{ user.email }}</p>
    </div>

    <h2>Modifier le profil</h2>
    <form action="/profile/update" method="post" enctype="multipart/form-data">
        <label for="username">Nom d'utilisateur:</label>
        <input type="text" id="username" name="username" value="{{ user.username }}">

        <label for="avatar">Avatar:</label>
        <input type="file" id="avatar" name="avatar">

        <button type="submit">Mettre à jour</button>
    </form>

    <a href="/">Retour à l'accueil</a>
</body>
</html>